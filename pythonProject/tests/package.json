{
  "name": "api-contract-tests",
  "version": "1.0.0",
  "description": "API contract validation tests for all microservices",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ajv": "^8.12.0",
    "vitest": "^1.0.0",
    "typescript": "^5.3.0"
  }
}
